# Быстрый старт SimpleBlog

## 🚀 Запуск приложения

### 1. Создайте .env файл
```bash
cp env.example .env
```

### 2. Запустите приложение
```bash
./run_dev.sh
```

Приложение будет доступно по адресу: http://localhost:8000

## 🧪 Тестирование

### Запуск всех тестов
```bash
./run_tests.sh
```

### Проверка API вручную
```bash
# Проверка health endpoint
curl http://localhost:8000/health

# Создание поста
curl -X POST http://localhost:8000/posts \
  -H "Content-Type: application/json" \
  -d '{"title": "Мой первый пост", "body": "Содержимое поста", "status": "draft"}'

# Получение списка постов
curl http://localhost:8000/posts
```

## 📚 Документация API

- **Swagger UI**: http://localhost:8000/docs
- **ReDoc**: http://localhost:8000/redoc

## 🔧 Разработка

### Установка зависимостей
```bash
pip install -r requirements.txt -r requirements-dev.txt
```

### Запуск линтеров
```bash
ruff --fix .
black .
isort .
```

### Запуск тестов
```bash
# Unit тесты
python -m pytest tests/unit/ -v

# Acceptance тесты (требуют запущенного API)
python -m pytest tests/acceptance/ -v
```

## 🐳 Docker

### Запуск с Docker Compose
```bash
docker-compose --profile dev up --build
```

### Остановка
```bash
docker-compose down
```

## 📁 Структура проекта

```
├── app/                    # Основной код приложения
│   ├── domain/            # Доменная логика
│   ├── storage/           # Слой данных
│   ├── transport/         # REST API
│   └── cmd/               # Точки входа
├── tests/                 # Тесты
│   ├── unit/              # Unit тесты
│   └── acceptance/        # Acceptance тесты
├── run_dev.sh             # Скрипт запуска
├── run_tests.sh           # Скрипт тестирования
├── env.example            # Пример переменных окружения
└── compose.yaml           # Docker Compose конфигурация
```

## 🆘 Решение проблем

### Порт 8000 занят
```bash
# Найдите процесс, использующий порт
lsof -i :8000

# Остановите процесс или измените порт в compose.yaml
```

### PostgreSQL не запускается
```bash
# Очистите volumes
docker-compose down -v

# Перезапустите
docker-compose --profile dev up --build
```

### Тесты не проходят
```bash
# Убедитесь, что API запущено
curl http://localhost:8000/health

# Запустите только unit тесты
python -m pytest tests/unit/ -v
```

## 📞 Поддержка

Если у вас возникли проблемы:
1. Проверьте, что все зависимости установлены
2. Убедитесь, что Docker запущен
3. Проверьте логи: `docker-compose logs`
4. Запустите тесты для диагностики: `./run_tests.sh`
